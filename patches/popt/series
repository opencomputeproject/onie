# This series applies on popt 1.19.
# The uClibc glob_pattern_p workaround and the aarch64 support patch are
# dropped: popt 1.19 detects glob_pattern_p via configure (handles its
# absence on uClibc) and carries aarch64 support upstream.
gitignore.patch
