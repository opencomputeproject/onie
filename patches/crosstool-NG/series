# Previous patches not applied on new revisions.
#allow-patching-custom-kernel-tarballs.patch
# Ignore-a-bad-id-command-to-allow-docker.patch -- merged upstream in 1.28.0
#   (scripts/crosstool-NG.sh already uses `id -un 2>/dev/null || echo unknown`).

