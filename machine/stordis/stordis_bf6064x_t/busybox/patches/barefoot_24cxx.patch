diff --git a/miscutils/24cXX.c b/miscutils/24cXX.c
index ccc0965..b574d8a 100644
--- a/miscutils/24cXX.c
+++ b/miscutils/24cXX.c
@@ -149,6 +149,7 @@ int eeprom_read_byte(struct eeprom* e, __u16 mem_addr)
 {
 	int r;
 	ioctl(e->fd, BLKFLSBUF); // clear kernel read buffer
+#if 0
 	if(e->type == EEPROM_TYPE_8BIT_ADDR)
 	{
 		__u8 buf =  mem_addr & 0x0ff;
@@ -163,6 +164,8 @@ int eeprom_read_byte(struct eeprom* e, __u16 mem_addr)
 	if (r < 0)
 		return r;
 	r = i2c_smbus_read_byte(e->fd);
+#endif
+	r = i2c_smbus_read_byte_data(e->fd, (__u8)mem_addr);
 	return r;
 }
 
-- 
2.7.4

