# Mount Win partition to get access to install archive.

mkdir -p /mnt/plexxi
echo "LABEL=Plexxi /mnt/plexxi vfat defaults 0 1" >> /etc/fstab
#mount /mnt/plexxi || "Info: could not mount Plexxi Win partition."

